document.addEventListener('DOMContentLoaded', function () {
  var popupBox = new SimpleLightbox('a.popupBox', {
    loop: false,
    doubleTapZoom: false,
    scrollZoom: false,
    showCounter: false,
  });
});