
  var popupBox = new SimpleLightbox('a.popupBox', {
    // var $gallery = new SimpleLightbox('.gallery a', {
    loop: false,
    doubleTapZoom: false,
    scrollZoom: false,
    showCounter: false,
  });


